- defaults:
    link:
      create: true
      relink: true
      force: false
      glob: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.config/git/config: XDG_CONFIG_HOME/git/config
    ~/.config/git/gitignore_global: XDG_CONFIG_HOME/git/gitignore_global
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.vim/snips/: vim/snips/*
    ~/.vim/.ycm_extra_conf.py: vim/ycm_extra_conf.py
